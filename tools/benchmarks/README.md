Movement benchmarks
===================

Run the microbenchmark to compare the `numba` and `numpy` implementations of movement kernels.

Requirements: optionally install `numba` to measure JIT-compiled performance.

Run locally:

```bash
python tools/benchmarks/movement_bench.py
```
