```markdown
# Long-term: Git client preference

- author: assistant
- timestamp: 2025-12-07
- tags: long_term, preferences, git

## Summary

The user prefers to use GitHub Desktop instead of the `git` CLI due to company policy. Record this preference so future instructions avoid recommending the `git` command-line unless the user explicitly says otherwise.

## Details

- Preference: `GitHub Desktop` (no `git` CLI available)
- Implication: Provide guidance and commands tailored to GUI workflows or explain equivalent GitHub Desktop steps when suggesting commits, branches, or PRs.

## Next steps for assistant

- When suggesting version-control actions, include GitHub Desktop steps and avoid raw `git` CLI commands by default.

```
