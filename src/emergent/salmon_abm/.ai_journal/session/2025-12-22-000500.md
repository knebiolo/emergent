Metadata:
- author: copilot
- timestamp: 2025-12-22T00:05:00Z
- tags: simulation, viewer, session

Files changed:
- src/emergent/salmon_abm/simulation.py (updated `run()` to support `viewer` and `viewer_blocking`)
- src/emergent/salmon_abm/realtime_viewer.py (new)
- setup.py (added `emergent-viewer` console script)
- RUNNING.md (added viewer usage instructions)
- src/emergent/salmon_abm/.ai_journal/session/2025-12-22-000000.md (earlier session)

Summary:
- Implemented an in-package realtime viewer and wired `simulation.run()` to optionally
  launch it. Added a console script `emergent-viewer` and documented usage in RUNNING.md.

Commands run:
- `python -m py_compile src/emergent/salmon_abm/realtime_viewer.py`
- `python -m py_compile src/emergent/salmon_abm/simulation.py`

Blockers:
- None. Runtime requires a GUI-capable environment and `PyQt5` + `h5py` installed.

Next steps:
- Add loader unit tests for a representative HDF5 layout.
- Optionally add a non-blocking viewer integration that can accept live-position updates via IPC.

What I wished I was told:
- Confirm whether the console script name `emergent-viewer` fits your naming preferences.
