Metadata:
- author: copilot
- timestamp: 2025-12-22T00:00:00Z
- tags: viewer, ui, session

Files changed:
- src/emergent/salmon_abm/realtime_viewer.py (new)

Summary:
- Added a Qt-based realtime viewer `realtime_viewer.py` to the `salmon_abm` package.
- The viewer loads HDF5 or CSV position traces and offers Start, Pause, Stop, Restart
  controls and a speed slider. Rendering uses `QOpenGLWidget` with `QPainter`.

Commands run:
- None (new file creation only). To run locally:
  ```powershell
  python -m emergent.salmon_abm.realtime_viewer path\to\outputs\sim_db_XXXX.h5
  ```

Blockers:
- Runtime requires `PyQt5` and `h5py` (for HDF5). The execution environment must have a GUI (Windows desktop session).

Next steps:
- Run quick smoke test to ensure module imports and UI open correctly on CI/dev machine.
- Complete CSV/HDF5 loader edge cases and tests.
- Update `requirements.txt` and documentation with the viewer usage instructions.

What I wished I was told:
- Confirm preferred default agent color/size and whether to overlay environment rasters.
