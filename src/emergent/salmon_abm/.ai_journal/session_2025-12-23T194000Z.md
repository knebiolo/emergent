# Session Report

Metadata:
- author: GitHub Copilot
- timestamp: 2025-12-23T19:40:00Z
- tags: live-viewer, nuyakuk, diagnostics, headless, raster

Files changed:
- [src/emergent/salmon_abm/realtime_viewer.py](src/emergent/salmon_abm/realtime_viewer.py)
- [tools/start_nuyakuk_live.py](tools/start_nuyakuk_live.py)
- Created: [src/emergent/salmon_abm/.ai_journal/session_2025-12-23T194000Z.md](src/emergent/salmon_abm/.ai_journal/session_2025-12-23T194000Z.md)

Summary:
- We integrated a live TCP streaming pipeline between the Nuyakuk ABM and a local Qt-based realtime viewer. The viewer now renders agent positions and a depth GeoTIFF background with transparent nodata. Streaming is working: frames are received continuously and logged.
- A visual problem was observed: all agents moved with a near-identical eastward velocity. That suggests an ABM internals issue (broadcasting/indexing or sampling) rather than a rendering problem. The next focus is to validate the ABM in a headless (UI-less) mode and collect per-agent traces for diagnosis.

Commands run (recommended repeatable commands):
- Start viewer (listen first):

```powershell
python -m emergent.salmon_abm.realtime_viewer --live --port 9001 --point-size 8 --env-depth data/salmon_abm/depth.tif --env-clip 2,98 --smooth-alpha 0.6 --debug
```

- Start streamer (after viewer):

```powershell
python tools/start_nuyakuk_live.py --port 9001 --nagents 200 --nsteps 1000 --fps 20
```

Blockers:
- Root-cause of identical eastward velocity is unknown. Could be a NumPy broadcasting bug, an indexing mistake in agent update loops, or an environment sampling error that returns the same vector for all agents.
- Intermittent socket errors (WinError 10061 / 10038) were observed earlier when the viewer and streamer startup order mismatched; starting the viewer first mitigates this.

Next steps:
- Implement a headless diagnostic runner (`tools/run_nuyakuk_headless.py`) that runs the sim without UI and writes per-timestep agent states to CSV/HDF5.
- Instrument `simulation.sample_environment` and the agent update step to record sampled flow vectors and computed agent velocities to the trace file (toggleable via `--diagnostic`).
- Run a short diagnostic sim (e.g., 200 agents, 200 timesteps) and analyze traces to locate where velocities collapse to the same vector.
- Add unit tests for raster sampling and the agent update loop to prevent regression.

Recent actions (this session):
- Added headless runner: [tools/run_nuyakuk_headless.py](tools/run_nuyakuk_headless.py) which imports available rasters into the sim HDF5, runs the sim headless, and writes a per-timestep per-agent CSV trace with sampled environment values and velocities.
- Prepared to run a quick diagnostic (50 agents × 50 timesteps) to generate a short trace for analysis; script path and output location are noted below.

Planned immediate run command (quick test):

```powershell
python tools/run_nuyakuk_headless.py --nagents 50 --nsteps 50 --model-name quick_headless_test --out outputs/diagnostics
```

Trace output path (expected):
- outputs/diagnostics/quick_headless_test_trace.csv

Quick run results:
- Trace generated: outputs/diagnostics/quick_headless_test_trace.csv
- Summary JSON: outputs/diagnostics/quick_headless_test_summary.json
- Finding: all agents share the same velocity vector every timestep — the summary reports `overall_unique_velocity_vectors: 1` and `any_timestep_single_unique_velocity: true`. This confirms the earlier UI symptom is produced by the simulation core, not the viewer.

Immediate next diagnostic:
- Instrument `movement.swim` and the movement plumbing so we can dump `dxdy`, `thrust`, `drag`, and sampled raster indices per timestep if needed. This will be the next step to locate where a scalar or broadcast is replacing per-agent arrays.

Notes / evidence locations:
- Viewer debug log: [viewer_debug.log](viewer_debug.log)
- Frame receipt trace: [viewer_frame_trace.txt](viewer_frame_trace.txt)
- Primary viewer code: [src/emergent/salmon_abm/realtime_viewer.py](src/emergent/salmon_abm/realtime_viewer.py)
- Streamer helper: [tools/start_nuyakuk_live.py](tools/start_nuyakuk_live.py)
- Simulation core (inspect for sampling & dynamics): [src/emergent/salmon_abm/simulation.py](src/emergent/salmon_abm/simulation.py)

End of session report.
